# ============================================================
# ReplicarIA - Railway Configuration
# ============================================================

[build]
builder = "nixpacks"
buildCommand = "python -m venv /opt/venv && . /opt/venv/bin/activate && cd backend && pip install -r requirements.txt"

[deploy]
startCommand = ". /opt/venv/bin/activate && cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
